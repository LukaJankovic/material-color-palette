'use babel';

import MaterialColorPickerView from '../lib/material-color-picker-view';

describe('MaterialColorPickerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
